.column(span="row")
    .section__title-wrapper
        .section-title= sectionTitle
.column(span="row" height="10")
        label(for="portrait").disc-red
            i.icon__youtube 
.column(span="row" height="10")
